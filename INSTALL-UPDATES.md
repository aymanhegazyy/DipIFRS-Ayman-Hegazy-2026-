# تحديث موقع DipIFRS

هذه الحزمة تضيف ثلاث وظائف إلى الموقع:

1. **PWA**: إمكانية تثبيت الموقع كتطبيق وتحسين التحميل والعمل من النسخة المخزنة مؤقتاً عند ضعف أو انقطاع الإنترنت.
2. **CBE Scratchpad**: مساحة ملاحظات وحساب سريع عائمة، مع حفظ الملاحظات تلقائياً على جهاز الطالب.
3. **Visitor Badge**: عداد زيارات مرئي في أسفل الصفحة.

## الملفات المطلوبة

ارفع الملفات التالية إلى جذر المستودع نفسه، بجوار `index.html` القديم:

- `index.html`
- `cbe-scratchpad.js`
- `manifest.json`
- `sw.js`
- `icon-192.png`
- `icon-512.png`

## طريقة الرفع من GitHub

1. افتح مستودع المشروع على GitHub.
2. اختر **Add file → Upload files**.
3. اسحب الملفات الستة إلى منطقة الرفع.
4. اختر **Commit changes**.
5. انتظر دقيقة أو دقيقتين ثم افتح رابط الموقع باستخدام `https://`.

إذا كان GitHub يعرض رسالة تأكيد استبدال `index.html`، اختر الاستبدال؛ فالنسخة الموجودة في هذه الحزمة تحتوي على المحتوى الأصلي بالإضافة إلى الإضافات الجديدة.

## اختبار الميزات

بعد فتح الموقع:

- يجب أن ترى أيقونة **📝** أسفل الشاشة؛ اضغط عليها لفتح CBE Scratchpad.
- اكتب ملاحظة، أغلق الصفحة، ثم افتحها مرة أخرى للتأكد من الحفظ المحلي.
- جرّب الحساب مثل `(5000 * 0.8) / 2`.
- تحقق من ظهور عداد الزيارات في أسفل الصفحة.
- في Chrome أو Edge، استخدم خيار **Install app / Add to Home Screen** إذا ظهر.

## ملاحظات مهمة

الملاحظات في Scratchpad محفوظة في `localStorage` على جهاز ومتصفح الطالب فقط؛ لا يتم إرسالها إلى خادم أو مشاركتها مع مستخدمين آخرين.

العمل دون اتصال يكون أفضل بعد زيارة الموقع مرة واحدة أثناء الاتصال بالإنترنت، حتى يتم تخزين ملفات التطبيق الأساسية. قد يحتاج المتصفح إلى إعادة تحميل واحدة بعد أول نشر حتى يلتقط Service Worker الجديد.

أيقونات PWA محلية داخل المستودع، ولذلك لا تعتمد على روابط خارجية. لا تحذف `icon-192.png` أو `icon-512.png` إذا أردت ظهور أيقونة التطبيق عند التثبيت.

عداد الزيارات يعتمد على خدمة خارجية؛ إذا توقف عن الظهور، فهذا لا يؤثر على عمل الموقع أو بقية الميزات.
